# DBMS
SQL  is used to manage and handle data in databases using queries. It includes commands for creating, updating, deleting, and retrieving data. Main concepts are DDL, DML, DCL, TCL, joins, constraints, and subqueries.
